pre-commit:
  parallel: true
  commands:
    oxfmt:
      glob: "*.{js,jsx,ts,tsx,md,mdx,css,json,jsonc,mjs,yml,yaml}"
      run: pnpm oxfmt {staged_files}
      stage_fixed: true
    oxlint:
      glob: "*.{js,jsx,ts,tsx,mjs,cjs}"
      run: pnpm oxlint --deny-warnings {staged_files}
