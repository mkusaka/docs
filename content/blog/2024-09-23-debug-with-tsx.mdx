---
title: "vscode で ts ファイルのデバッグをする際に `javascript debug terminal` で `node --import tsx main.ts` をすると便利"
date: 2024-09-23
description: "vscode で ts ファイルのデバッグをする際に `javascript debug terminal` で `node --import tsx main.ts` をすると便利"
categories: [tips]
tags: [node, vscode, tsx, typescript, debug]
---

## まとめ

- vscode で ts ファイルのデバッグをする際に `javascript debug terminal` で `node --import tsx main.ts` をすると debugger つかえて便利
  - `npm i tsx` は必要
  - `javascript debug terminal` は以下から起動できる

![terminal で javascirpt debug terminal を起動する様子](/img/debug-with-tsx.png)
